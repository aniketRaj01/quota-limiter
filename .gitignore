venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
*.egg-info/

# Load test output — reproducible on demand via load_test/load_test.py, not committed.
load_test/results.txt

# Docs — working drafts, not ready to commit yet. README.md and DESIGN.md are the
# actual deliverables and are tracked; everything else (*.md/*.pdf) stays local.
*.md
*.pdf
!README.md
!DESIGN.md
